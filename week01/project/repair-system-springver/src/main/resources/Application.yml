spring:
  datasource:
    url: jdbc:mysql://localhost:3306/repair_system?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.dorm.entity

server:
  port: 8080
# JWT 配置
jwt:
  secret: your-secret-key-here-at-least-256-bits-long
  expiration: 86400000  # 24小时，单位毫秒